# Mode mock : réponses fictives, pas besoin de serveur modèle
# true = active les réponses fictives (dev sans serveur)
# false ou absent = appelle le vrai serveur modèle
VITE_MOCK=false

# URL du serveur modèle (par défaut http://localhost:8000)
VITE_MODEL_SERVER_URL=http://localhost:8000
