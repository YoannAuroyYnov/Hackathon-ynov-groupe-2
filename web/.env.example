# Mode mock : réponses fictives, pas besoin de Triton
# true = active les réponses fictives (dev sans serveur)
# false ou absent = appelle le vrai serveur Triton
VITE_MOCK=false

# URL du serveur Triton (par défaut http://localhost:8000)
VITE_TRITON_URL=http://localhost:8000
